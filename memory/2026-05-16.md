# 2026-05-16 Daily Log

## job_wiki 파이프라인 구축 완료 🦀

### 자동화 범위 (최종)
GitHub Actions 파이프라인이 **수집 → 분석 → Wiki 변환 → 온톨로지 병합 → 만료 정리 → 푸시**까지 전부 자동으로 돌아가게 됨.

### 주요 변경사항
1. **merge_ontology.py** — Suggested_Keywords.json → Ontology_Map.json 자동 병합 스크립트
2. **cleanup_expired.py** — ALIO API `pbancEndYmd` 기준 만료 공고 자동 정리 (Raw, Archive, Index, Wiki Analysis, Company Profile 전부)
3. **llm_client.py** — 온톨로지 컨텍스트 포함하도록 프롬프트 강화
4. **harvest.yml** — 4-step 워크플로우 (cleanup → harvest → wiki → merge → push)
5. **analyzer.py** — Groq provider 지원 추가 (기존엔 nvidia/openai만 있음)
6. **API URL 수정** — `openapi/service/rest/RecruitService/getRecruitList` → `new/v1/recruit/list.do`

### 트러블슈팅
- `--mock` 인자 버그 (boolean vs string 비교) → `!inputs.mock`으로 수정
- `ALIO_ENDPOINT` 기본값 누락 → workflow env에 기본 URL 추가
- GITHUB_TOKEN write 권한 누락 → `permissions: contents: write` 추가
- 분석 단계 Groq 미연동 → analyzer.py에 groq provider 추가, `LLM_PROVIDER=groq` 설정

### 비용 최적화 전략
- Groq `llama-4-scout-17b-16e-instruct` 사용 (TPM 300K, $0.11/M tok)
- `DETAIL_MAX_DETAIL_CALLS=50` (50건만 상세 분석)
- `ANALYSIS_MIN_CHARS_TO_CALL_LLM=300` (300자 이상만 LLM 호출)
- `NCS_FILTER_MODE=off` (범용, 필터 없음)
- 예상 비용: ~$0.25/회 (무료 크레딧 커버)

### 현재 repo 상태
- job_wiki: 999건 공고 수집 완료 (cleanup으로 19건 만료 정리)
- ALIO API 실제 연결 성공
- 매주 월요일 09:00 KST 자동 실행

### 주요 레퍼런스
- ALIO API URL: `https://opendata.alio.go.kr/new/v1/recruit/list.do`
- Groq 모델: https://console.groq.com/docs/models

## MetaGPT + EJClaw + gstack 통합 프로젝트

khmo가 공유한 문서 기반으로 세 가지 오픈소스를 결합하는 아키텍처 설계

**컨셉:**
- MetaGPT (본사 기획부) - Python 기반, PM/Architect 역할, PRD→설계 생성
- EJClaw (외주 개발팀) - Bun 기반, Owner/Reviewer/Arbiter 3인 Tribunal
- gstack (엘리트 두뇌) - Garry Tan의 23개 Claude Code Skill, 엘리트 프롬프트 팩
- OpenClaw (Claw = 나) - 트리아지/오케스트레이션/ENV 관리

**아키텍처 문서 작성:** workspace/ARCHITECTURE_metaGPT_EJClaw_gstack.md

**다음 단계:** khmo 리뷰 후 Phase 1 구현 시작

## MetaGPT + EJClaw + gstack - Architecture Update

### 수정 사항 (khmo 피드백 반영)
1. **API 호환성**: OpenCode Go (DeepSeek V4 Pro/Flash) + Copilot Edu Pro 모두 대응
2. **컨테이너**: MetaGPT (Python) + EJClaw (Bun) 각각 격리, Docker 네트워크로 연결, 공유 볼륨으로 통신
3. **모델 매트릭스**: 역할별 최적 모델 지정
   - 기획/설계/코딩/중재 → DeepSeek V4 Pro (OpenCode Go)
   - 리뷰/QA/일상 → DeepSeek V4 Flash (OpenCode Go)
   - Copilot Edu Pro는 GPT-5.3-Codex, Claude Opus 4.6 등 2순위로
4. **BLOCKED/HITL**: 4가지 케이스별 처리 프로토콜 상세화
   - ENV 부족 → Claw 중계
   - Arbiter ESCALATE → khmo 결정
   - 반복 BLOCKED 루프 감지 → 자동 보고
   - 비용 한도 도달 → fallback 제안

### 문서 위치
- workspace/ARCHITECTURE_metaGPT_EJClaw_gstack.md (v2)
# 2026-05-16 Daily Log

## job_wiki 파이프라인 구축 완료 🦀

### 자동화 범위 (최종)
GitHub Actions 파이프라인이 **수집 → 분석 → Wiki 변환 → 온톨로지 병합 → 만료 정리 → 푸시**까지 전부 자동으로 돌아가게 됨.

### 주요 변경사항
1. **merge_ontology.py** — Suggested_Keywords.json → Ontology_Map.json 자동 병합 스크립트
2. **cleanup_expired.py** — ALIO API `pbancEndYmd` 기준 만료 공고 자동 정리 (Raw, Archive, Index, Wiki Analysis, Company Profile 전부)
3. **llm_client.py** — 온톨로지 컨텍스트 포함하도록 프롬프트 강화
4. **harvest.yml** — 4-step 워크플로우 (cleanup → harvest → wiki → merge → push)
5. **analyzer.py** — Groq provider 지원 추가 (기존엔 nvidia/openai만 있음)
6. **API URL 수정** — `openapi/service/rest/RecruitService/getRecruitList` → `new/v1/recruit/list.do`

### 트러블슈팅
- `--mock` 인자 버그 (boolean vs string 비교) → `!inputs.mock`으로 수정
- `ALIO_ENDPOINT` 기본값 누락 → workflow env에 기본 URL 추가
- GITHUB_TOKEN write 권한 누락 → `permissions: contents: write` 추가
- 분석 단계 Groq 미연동 → analyzer.py에 groq provider 추가, `LLM_PROVIDER=groq` 설정

### 비용 최적화 전략
- Groq `llama-4-scout-17b-16e-instruct` 사용 (TPM 300K, $0.11/M tok)
- `DETAIL_MAX_DETAIL_CALLS=50` (50건만 상세 분석)
- `ANALYSIS_MIN_CHARS_TO_CALL_LLM=300` (300자 이상만 LLM 호출)
- `NCS_FILTER_MODE=off` (범용, 필터 없음)
- 예상 비용: ~$0.25/회 (무료 크레딧 커버)

### 현재 repo 상태
- job_wiki: 999건 공고 수집 완료 (cleanup으로 19건 만료 정리)
- ALIO API 실제 연결 성공
- 매주 월요일 09:00 KST 자동 실행

### 주요 레퍼런스
- ALIO API URL: `https://opendata.alio.go.kr/new/v1/recruit/list.do`
- Groq 모델: https://console.groq.com/docs/models

## MetaGPT + EJClaw + gstack 통합 프로젝트 — Phase 1 진행중

### 최종 아키텍처 결정사항

**컨테이너 구성:**
- MetaGPT 컨테이너 → Copilot Edu Pro (GitHub Models API) → GPT-5.4-mini-high
- EJClaw 컨테이너 → OpenCode Go → DeepSeek V4 Pro → GLM-5.1 → Qwen3.6 Plus → Kimi K2.6
- 두 컨테이너는 각각 격리, Docker 네트워크(aifactory-net)로 연결, 공유 볼륨(workspace-data)으로 설계서/코드 교환

**최종 모델 매트릭스 (2026-05-16 확정):**

| 역할 | 모델 | Provider |
|------|------|----------|
| MetaGPT PM/Architect | GPT-5.4-mini-high | Copilot Edu Pro |
| EJClaw Owner | GLM-5.1 | OpenCode Go |
| EJClaw Reviewer | Qwen3.6 Plus | OpenCode Go |
| EJClaw Arbiter | Kimi K2.6 | OpenCode Go |
| QA/테스트 | GPT-5.3-Codex | Copilot Edu Pro |

- Reviewer를 V4 Flash에서 Qwen3.6 Plus로 업그레이드 (khmo 판단: Reviewer도 코드 분석/검증 중요)
- PM/Architect를 Copilot으로 옮겨서 API 부하 분산
- OpenCode Go($10/mo) + Copilot Edu Pro(보유) — 추가 결제 없음

**BLOCKED/HITL 설정:**
- 동일 BLOCKED 반복 임계값: 2회 (3→2로 하향)
- 4가지 HITL 케이스: ENV 부족 / Arbiter ESCALATE / 반복 BLOCKED 루프 / 비용 한도
- Arbiter 판결 옵션: PROCEED, REVISE, RESET, ESCALATE

**second_brain + Hermes 역할 정립:**
- second_brain = 회사 문서 보관함 (외부 문서 구조화, 분류/링크, 그래프)
- Hermes = 문서 보관함 관리자 + 과거 맥락 기억
- AI Factory 산출물(PRD, 설계서, 코드 diff)은 전부 second_brain에 기록
- 기록 명령은 Claw → Hermes로 전달 (Khmo 직접 기록 요청 불필요)

### Phase 1 진행 상태
- ✅ 아키텍처 문서 v3 완성 (workspace/ARCHITECTURE_metaGPT_EJClaw_gstack.md)
- ✅ docker-compose.yml (MetaGPT + EJClaw, network + shared volume)
- ✅ MetaGPT Dockerfile + entrypoint + config (Copilot API)
- ✅ EJClaw Dockerfile + entrypoint + room_settings (OpenCode Go)
- ✅ .env.example + aifactory.sh 관리 스크립트
- 🔄 MetaGPT 컨테이너 빌드중 (pip install metagpt 백트래킹 이슈 해결중)
- ⏳ EJClaw 컨테이너 빌드 대기

### 미완료/보류
- Gemini 2.5 Pro / 3.1 Pro → Copilot Edu Pro에서 공식 지원되는지 불확실. 보류.
- Hermes ↔ second_brain 연결 설정 (AI Factory 공유 볼륨 마운트) — Phase 2로 연기
- gstack 프롬프트 이식 (PM/Reviewer/Architect) — Phase 2로 예정

## Session 종료 (13:26 KST) — 전체 완료

### 오늘 한 일
**Phase 1-3 전체 구축 완료** — AI Factory (MetaGPT + EJClaw + gstack + OpenClaw + Hermes)

1. **컨테이너 3개 구축**
   - aifactory-metagpt (3.3GB) — 기획/설계/QA, DeepSeek V4 Pro
   - aifactory-ejclaw (2.54GB) — 구현/리뷰, GLM-5.1/Qwen3.6 Plus/Kimi K2.6
   - hermes (기존) — 문서 관리, DeepSeek V4 Flash

2. **Provider 설정**
   - OpenCode Go ($10/mo) — 모든 메인 작업
   - Copilot Edu Pro (보유) — QA 전용
   - GitHub PAT: `models` 권한 필요 (미설정)

3. **gstack 프롬프트 이식** — 4종
4. **Two-Track 문서 전략** 수립 (메인=덮어쓰기, 결정=Append)
5. **Full Pipeline** 순서 정립: MetaGPT→EJClaw→QA→Claw보고→khmo→Hermes
6. **디스크 정리:** second_brain(9GB) + hermes-agent(8.5GB) + job_wiki(18MB) 제거 → 17.5GB 확보
7. **API 키 저장 완료** (TOOLS.md + .env)
